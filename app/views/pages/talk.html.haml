= render :partial => "index_header"
.main-container
  .jumbotron
    .session_select
      %h1 I want to talk
      - if current_user.present?
        %a.btn.btn-primary.btn-lg{:href => ready_user_path(current_user), :role => "button", :"data-method" => :put} to anyone who is online 
      - else 
        %a.btn.btn-primary.btn-lg{:href => users_path, :role => "button"} to anyone who is online 
      %a.btn.btn-primary.btn-lg{:href => "listener_search", :role => "button"} to someone who I choose
      %a.btn.btn-primary.btn-lg{:href => "listener_search", :role => "button"} some times later

= render :partial => "footer"
