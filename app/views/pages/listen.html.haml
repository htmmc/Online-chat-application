= render :partial => "index_header"
.main-container
  .jumbotron
    .session_select
      %h1 I want to help by listening to
      .row.buttons
        %a.btn.btn-primary.btn{:href => chat_session_path, :role => "button"} anyone who is online now
        - if current_user.present?
          %a.btn.btn-primary.btn{:href => user_path(current_user), :role => "button"} someone with a particular subject in mind
        - else 
          %a.btn.btn-primary.btn{:href => new_user_registration_path, :role => "button"} someone with a particular subject in mind

= render :partial => "footer"
